import React, { useState } from "react";

const SideBar = () => {
  const [isActive, setIsActive] = useState('dashboard');

  return (
    <div>
      <div>SideBar</div>
    </div>
  );
};

export default SideBar;
